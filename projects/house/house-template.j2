<h1>Architecture</h1>

<div id="carousel-1" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
    {% for item in images %}
        {% if loop.index is equalto 1 %}
            <li data-target="#carousel-1" data-slide-to="{{ loop.index }}" class="active"></li>
        {% else %}
            <li data-target="#carousel-1" data-slide-to="{{ loop.index }}"></li>
        {% endif %}
    {% endfor %}
    </ol>
    
    <div class="carousel-inner" role="listbox">
    {% for item in images %}
        {% if loop.index is equalto 1 %}
            <div class="item active">
                <img src="{{item}}" alt="{{item}}">
            </div>
        {% else %}
            <div class="item">
                <img src="{{item}}" alt="{{item}}">
            </div>
        {% endif %}
    {% endfor %}
    </div> <!-- carousel-inner -->


    <a class="left carousel-control" href="#carousel-1" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#carousel-1" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div> <!-- carousel-->

<br>

<p>I took four years of drafting classes in high school and became proficient in hand drafting, Inventor, AutoCAD, and Revit.  Prior to graduation, I designed a house, including all of the construction drawings.</p>